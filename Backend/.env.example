# Database Configuration
DB_HOST=172.16.20.6
DB_USER=root
DB_PASSWORD=Karma@1107
DB_NAME=opticonnectgis_db
DB_PORT=3306

# JWT Configuration
JWT_SECRET=opticonnectgis_secret_key_change_in_production
JWT_EXPIRE=15m
JWT_REFRESH_EXPIRE=7d

# Server Configuration
PORT=82
NODE_ENV=development

# CORS - Frontend URL
FRONTEND_URL=http://localhost:3005

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Email Configuration (Gmail SMTP)
# Use Gmail for sending verification emails
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_SECURE=false
EMAIL_USER=optimaltelemedia.verify.mail@gmail.com
EMAIL_PASSWORD=fdgr wbow vbdr vkug
EMAIL_FROM="OptiConnect GIS <optimaltelemedia.verify.mail@gmail.com>"

# Alternative: Microsoft 365 / Outlook SMTP (requires SMTP AUTH enabled by IT)
# EMAIL_HOST=smtp.office365.com
# EMAIL_USER=himil.chauhan@optimaltele.net

# Application URL (for email verification links)
APP_URL=http://localhost:3005
